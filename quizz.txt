<!DOCTYPE html>
<html lang="en">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>marine life quizz</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="styles.css" rel="stylesheet">
  <style>
    body {
      background-color: #e0f7fa;
      font-family: 'Segoe UI', sans-serif;
      color: #004d40;
    }

    .arrow {
      font-size: 6rem;
      color: #00796b;
      animation: pulse 2s infinite;
    }

    .animate-fade {
      animation: fadeInUp 1s ease forwards;
      opacity: 0;
    }

    @keyframes fadeInUp {
      from {
        transform: translateY(40px);
        opacity: 0;
      }
      to {
        transform: translateY(0);
        opacity: 1;
      }
    }

    @keyframes pulse {
      0%, 100% {
        transform: scale(1);
      }
      50% {
        transform: scale(1.2);
      }
    }
  </style>
</head>

<body>
  <section id="quiz" class="py-5 bg-gradient-to-r from-blue-100 to-blue-300">
    <div class="container max-w-4xl mx-auto px-4">
      <div class="bg-white shadow-xl rounded-2xl p-8">
        <h2 class="text-3xl font-bold text-center text-blue-800 mb-4 animate-bounce">🧠 Marine Life Quiz</h2>
        <p class="text-center text-lg text-gray-700 mb-6">Test your knowledge about marine species and ocean conservation. 🌊</p>
        <form id="marineQuiz">
          <ol class="space-y-6">
            <!-- 20 Questions Here (unchanged for brevity) -->
            <!-- Copy-paste all your existing <li> questions here as-is -->
            <!-- ... -->
           
          <!-- ✅ Quizizz Button Added Below -->
          <div class="text-center mt-4">
            <a href="https://wayground.com/pro/join?gc=52895420" class="btn btn-primary px-4 py-2 rounded">
              🔗 Play Quizz on the Quizizz
            </a>
          </div>

        </form>
      </div>
    </div>
  </section>

  <script>
    document.getElementById("marineQuiz").addEventListener("submit", function(event) {
      event.preventDefault();
      const form = event.target;
      let score = 0;
      for (let i = 1; i <= 20; i++) {
        const answer = form[`q${i}`];
        if (answer) {
          for (let j = 0; j < answer.length; j++) {
            if (answer[j].checked && answer[j].value === "1") {
              score++;
            }
          }
        }
      }
      const feedback = document.getElementById("score");
      feedback.innerText = `🎉 You scored ${score} out of 20!`;
      feedback.style.animation = "pulse 1s ease-in-out infinite alternate";
    });
  </script>


    </a>
  </div>
</body>
</html>